import crypto from 'crypto';
import { command } from 'yargs';

const argv = command('keygen', 'Genterate encryption key for server and cloudflare api key encrypted while send request to server', {
          
}